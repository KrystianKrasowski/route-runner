if(TARGET_ARCH STREQUAL "test")
    add_subdirectory(stm32f303x8_stub)
    add_subdirectory(core)
    add_subdirectory(utils)
    add_subdirectory(drivers)
    add_subdirectory(mocks)
    # add_subdirectory(adapters)
endif()